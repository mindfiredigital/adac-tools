export const commonSuggestions = {
  enableEncryption:
    "Ensure 'encrypted' is set to true in the service configuration.",
  privateSubnet:
    'Place the service inside a private subnet to prevent direct internet exposure.',
  enableLogging: 'Enable access logs to ensure traceability of requests.',
  leastPrivilege:
    'Avoid wildcard actions in IAM policies to enforce least privilege principles.',
};
